#include "defs.h"
#include "interface.h"


void menu(int state)
{
   // pretend that this displays a menu
   int m = START;
   int i;
   for ( i = 0; i < state; i++ )
       /* do something */;
}
